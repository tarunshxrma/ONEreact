export const AddColorGreen = (props) => {
    return (
        <div style={{backgroundColor:'green', height:'50px'}}>{props.value}
        <br/><br/>
        </div>
    )
}

export const AddColorRed = (props) => {
    return (
        <div style={{backgroundColor:'red', height:'50px'}}>{props.value}</div>
    )
}